[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dynasty-model"
version = "0.1.0"
description = "NBA Fantasy Dynasty Player Valuation Engine"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["src*"]
